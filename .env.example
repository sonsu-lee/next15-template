# No environment variables are required by this template yet.
#
# Do not define NODE_ENV here. Next.js sets it automatically for dev/build,
# and test runners set it for test runs.
#
# When adding a required variable, document it here with a safe sample value,
# then copy this file to .env.local for local development overrides.
